{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // Workaround for ESM/CJS interop issues: the root tsconfig.json currently transpiles to CJS,
    // but config.ts imports ESM dependencies. So let TS know it doesn't need to worry about module
    // format compat issues (since the docs are actually built with Vite).
    "module": "preserve",
    "moduleResolution": "bundler"
  },
  "include": ["./.vuepress/*"]
}
