// Empty Yarn plugin stub. Never actually loaded (see package.json description),
// but exports a valid no-op plugin shape just in case.
module.exports = {
  name: '@yarnpkg/plugin-stub',
  factory: () => ({}),
};
