export function publish(cwd?: string) {
  cwd = cwd || process.cwd();

  // bump the version
  // npm / yarn publish
  // reset
  // fetch and merge from latest
  // bump the version
  // checkin
  // push
}
