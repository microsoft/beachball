enableScripts: false

nodeLinker: node-modules

npmMinimalAgeGate: 7d

npmPreapprovedPackages: []

# Include local yarn plugins (bundled) as an E2E test.
# If something breaks and the bundles are deleted, you may need to temporarily comment this out.
# Usage outside beachball should use the official plugin versions as described in docs, e.g.
# https://github.com/microsoft/beachball/blob/main/yarn-plugins/npmrc/README.md
plugins:
  # yarn-plugin-engines enforces the repo-root engines.node requirement across dependencies.
  - path: yarn-plugins/engines/dist/plugin.js
  # yarn-plugin-npmrc simplifies auth for the release pipeline by reusing .npmrc.
  # It's enabled (YARN_NPMRC_AUTH_ENABLED) only for ADO publish by .ado/release.yml.
  - path: yarn-plugins/npmrc/dist/plugin.js
  - checksum: 8d8252c376e41a67ca509b2778f3fb92164aa91828459ae8a6aa0e13f1b6f9843586d0983ba9a200f6d813d37efc171474c8dbc85b0dff01df56c0d476c238ce
    path: .yarn/plugins/@rnx-kit/yarn-plugin-ignore.cjs
    spec: 'https://raw.githubusercontent.com/microsoft/rnx-kit/main/incubator/yarn-plugin-ignore/dist/yarn-plugin-ignore.cjs'

preferReuse: true

yarnPath: .yarn/releases/yarn-4.17.1.cjs

catalogs:
  prod:
    '@yarnpkg/core': ^4.7.0
    commander: ^14.0.3
    nano-spawn: '^2.1.0'
    semver: ^7.7.4
    workspace-tools: ^0.42.0
  dev:
    '@types/semver': ^7.3.13
    cross-env: ^10.1.0
    eslint: ^10.0.0
    # tsgo provides the `tsc` binary
    ts7: npm:typescript@~7.0.0
    # compat package provides require("typescript") for typescript-eslint
    typescript: npm:@typescript/typescript6@^6.0.0
